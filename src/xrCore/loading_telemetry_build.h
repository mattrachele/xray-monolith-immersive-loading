#pragma once

// GitHub Actions replaces this file in the build workspace with the exact
// commit being compiled. Local builds retain an explicit development marker.
#define LOADING_TELEMETRY_ENGINE_COMMIT "development"
